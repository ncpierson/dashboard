import React from 'react';
import Widgets from './Widgets';

const App = () => {
  return (
    <main>
      <section id="header">nick, exposed</section>
      <Widgets />
    </main>
  );
};

export default App;
